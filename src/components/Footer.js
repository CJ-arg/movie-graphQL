import React from "react";

const Footer = () => {
  return (
    <div className="footer-container">
      <p className="footer">Movie GraphQL {new Date().getFullYear()}</p>
    </div>
  );
};

export default Footer;
